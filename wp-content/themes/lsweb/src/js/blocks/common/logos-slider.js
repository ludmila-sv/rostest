( function ( $ ) {
	/*
	if ( $( '.logos-slider__list' ).length ) {
		$( '.logos-slider__list' ).owlCarousel( {
			items: 6,
			// autoWidth:true,
			loop: true,
			nav: false,
			dots: false,
			margin: 20,
			autoplay: true,
			smartSpeed: 5000,
			autoplayTimeout: 5000,
			autoplayHoverPause: false,
			slideTransition: 'linear',
			responsive: {
				0: {
					items: 2,
				},
				992: {
					items: 4,
				},
				1400: {
					items: 6,
				},
			},
		} );
	}
	*/
} )( jQuery );
